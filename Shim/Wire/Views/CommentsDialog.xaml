<Window x:Class="TurboSuite.Wire.Views.CommentsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Circuit Comments"
        Width="490" SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        FontFamily="Segoe UI" FontSize="11">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/TurboSuite;component/Shared/Styles/TurboSuiteStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <StackPanel Margin="16">
        <Grid Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="PromptText" Text="Enter circuit comment:"
                       Grid.Column="0" Grid.Row="0" Margin="0,0,0,8" />
            <TextBlock Text="Room Override:"
                       Grid.Column="1" Grid.Row="0" Margin="8,0,0,8" />
            <TextBlock Text="Panel:"
                       Grid.Column="2" Grid.Row="0" Margin="8,0,0,8" />
            <ComboBox x:Name="CommentsComboBox"
                      Grid.Column="0" Grid.Row="1"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      StaysOpenOnEdit="True" />
            <ComboBox x:Name="RoomOverrideComboBox"
                      Grid.Column="1" Grid.Row="1"
                      IsEditable="True"
                      IsTextSearchEnabled="True"
                      StaysOpenOnEdit="True"
                      MinWidth="110"
                      Margin="8,0,0,0" />
            <ComboBox x:Name="PanelComboBox"
                      Grid.Column="2" Grid.Row="1"
                      DisplayMemberPath="Name"
                      MinWidth="60"
                      Margin="8,0,0,0" />
        </Grid>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,8,0"
                    Style="{StaticResource RevitButtonStyle}"
                    IsDefault="True" Click="OkButton_Click" />
            <Button Content="Cancel" Width="75"
                    Style="{StaticResource RevitButtonStyle}"
                    IsCancel="True" />
        </StackPanel>
    </StackPanel>
</Window>
